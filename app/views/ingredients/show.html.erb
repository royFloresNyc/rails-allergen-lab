<h1><%= @ingredient.name %></h1>
<h3><em>Included in:</em></h3>
<ul>
    <% @ingredient.recipes.each do |recipe| %>
        <li><%= link_to recipe.title, recipe_path(recipe) %></li>
    <% end %>
</ul>