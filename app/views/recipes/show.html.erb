<h1><%= @recipe.title %></h1>
<p>Submited by: <em><%= link_to @recipe.user.name, user_path(@recipe.user) %></em></p>
<h3>Ingredients:</h3>
<ul>
    <% @recipe.ingredients.each do |ingredient| %>
        <li><%= link_to ingredient.name, ingredient_path(ingredient) %></li>
    <% end %>
</ul>